spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/catalog}
    username: ${SPRING_DATASOURCE_USERNAME:e-mall}
    password: ${SPRING_DATASOURCE_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true
      org:
        hibernate:
          SQL: DEBUG
          type: TRACE


  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:capybara.lmq.cloudamqp.com}
    port: ${SPRING_RABBITMQ_PORT:5671}
    username: ${SPRING_RABBITMQ_USERNAME:nyrpkbjz}
    virtual-host: ${SPRING_RABBITMQ_VHOST:nyrpkbjz}
    password: ${SPRING_RABBITMQ_PASSWORD:321DkyforAD_AJIuPGZd56wi_6rAKiVq}
    ssl:
      enabled: true

server:
  port: ${SPRING_PORT:8081}
